#include <stdio.h>
#include <string.h>

int main() {
    char str[100];
    int sum = 0;

    printf("Enter the string : ");
    gets(str);

    for(int i = 0; i < strlen(str); i++) {
        sum += (int)str[i];
    }

    printf("Sum of all characters : %d", sum);
    return 0;
}
